apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-plactice.fullname" . }}
  labels:
    {{- include "helm-plactice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-plactice.selectorLabels" . | nindent 4 }}
