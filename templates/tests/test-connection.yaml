apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-plactice.fullname" . }}-test-connection"
  labels:
    {{- include "helm-plactice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-plactice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
